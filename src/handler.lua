local http = require('socket.http')
local dev = require('dev')
local ltn12 = require('ltn12')
for _,v in next, callback(comment) do 
  wait(2)
  table.concat(dev.callback2[1])
end

local call = io.read()

wait(2)

io.write(call, "answer") 
call.append(#callback2[math.sin(-1, +1)])


